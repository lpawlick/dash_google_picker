{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/usage.py",
            "preLaunchTask": "npm run build"
        }
    ]
}