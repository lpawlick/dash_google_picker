{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm run build",
            "type": "shell",
            "command": "export NODE_OPTIONS=--openssl-legacy-provider && npm run build",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
